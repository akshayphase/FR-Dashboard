import { GuardSearchPipePipe } from './search-pipe.pipe';

describe('GuardSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GuardSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
